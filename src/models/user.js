export default class User {
    constructor(lastName, firstName, email, gpPassword) {
        this.lastName = lastName;
        this.firstName = firstName;
        this.email = email,
            this.gpPassword = gpPassword;
    }
}